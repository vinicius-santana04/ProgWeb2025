<div class="container my-5" style="max-width: 600px;">
    <h2>Alterar Senha</h2>
    <p class="text-muted">Para sua segurança, informe sua senha atual antes de definir uma nova.</p>

    {{#if error}}
        <div class="alert alert-danger">{{error}}</div>
    {{/if}}
    {{#if success}}
        <div class="alert alert-success">{{success}}</div>
    {{/if}}

    <form action="/users/password" method="POST" class="mt-4">
        <div class="form-floating mb-3">
            <input type="password" class="form-control" id="currentPassword" name="currentPassword" required>
            <label for="currentPassword">Senha Atual</label>
        </div>
        <hr>
        <div class="form-floating mb-3">
            <input type="password" class="form-control" id="newPassword" name="newPassword" required>
            <label for="newPassword">Nova Senha</label>
        </div>
        <div class="form-floating mb-4">
            <input type="password" class="form-control" id="passwordConfirmation" name="passwordConfirmation" required>
            <label for="passwordConfirmation">Repetir Nova Senha</label>
        </div>
        <div class="d-grid gap-2">
             <button type="submit" class="btn btn-primary btn-lg">Alterar Senha</button>
             <a href="/lobby" class="btn btn-outline-secondary">Cancelar</a>
        </div>
    </form>
</div>