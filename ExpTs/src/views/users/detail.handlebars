{{#if user}}
<div class="container my-5">
    <div class="card shadow-sm">
        <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0">Detalhes do Usuário</h4>
            <a href="/users" class="btn btn-outline-light btn-sm">
                <i class="bi bi-arrow-left-circle-fill me-1"></i> Voltar para a Lista
            </a>
        </div>
        <div class="card-body p-4">
            <h2 class="card-title display-6">{{user.name}}</h2>
            <h6 class="card-subtitle mb-4 text-muted">
                <i class="bi bi-envelope-fill me-1"></i> {{user.email}}
            </h6>
            
            <dl class="row mt-4">
                <dt class="col-sm-3">Curso:</dt>
                <dd class="col-sm-9">{{user.major.name}}</dd>

                <dt class="col-sm-3">Pontuação Máxima:</dt>
                <dd class="col-sm-9">{{user.highScore}}</dd>
            </dl>
        </div>
        <div class="card-footer bg-light text-muted d-flex justify-content-between align-items-center">
            <div>
                <small>Criado em: {{user.createdAt}}</small>
                <br>
                <small>Última atualização: {{user.updatedAt}}</small>
            </div>
            <div class="d-flex gap-2">
                <a href="/users/update/{{user.id}}" class="btn btn-warning" title="Editar Usuário">
                    <i class="bi bi-pencil-fill"></i> Editar
                </a>
                <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#modal-delete-user-{{user.id}}" title="Excluir Usuário">
                    <i class="bi bi-trash-fill"></i> Excluir
                </button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="modal-delete-user-{{user.id}}" tabindex="-1" aria-labelledby="modalLabel-{{user.id}}" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="modalLabel-{{user.id}}">Confirmar Exclusão</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <p>Você tem certeza que deseja excluir o usuário <strong>{{user.name}}</strong>?</p>
        <p class="text-danger"><small>Esta ação não pode ser desfeita.</small></p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
        <button type="button" class="btn btn-danger" onclick="excluirItemEredirecionar('users', '{{user.id}}', '/users')">Sim, excluir</button>
      </div>
    </div>
  </div>
</div>

{{else}}
<div class="alert alert-danger text-center" role="alert">
    <h4 class="alert-heading">Usuário não encontrado!</h4>
    <p>O usuário que você está tentando acessar não existe ou foi removido.</p>
    <hr>
    <a href="/users" class="btn btn-secondary">Voltar para a Lista de Usuários</a>
</div>
{{/if}}
